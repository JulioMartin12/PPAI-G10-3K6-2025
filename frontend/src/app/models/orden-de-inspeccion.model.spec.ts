import { OrdenDeInspeccion } from './orden-de-inspeccion.model';

describe('OrdenDeInspeccion', () => {
  it('should create an instance', () => {
    expect(new OrdenDeInspeccion()).toBeTruthy();
  });
});
